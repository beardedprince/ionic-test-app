import { ActivityVideoPipe } from './activity-video.pipe';

describe('ActivityVideoPipe', () => {
  it('create an instance', () => {
    const pipe = new ActivityVideoPipe();
    expect(pipe).toBeTruthy();
  });
});
